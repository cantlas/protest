import React from 'react'
import PropTypes from 'prop-types'

const SearchBox = () => (
    <div>
      <input type="text" placeholder="Search for a protest..."></input>
      <button></button>
    </div>
)
